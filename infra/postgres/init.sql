-- The "booking" database is created by POSTGRES_DB. Each service owns its own
-- database; this creates the rest. Schema lives with the owning service.
CREATE DATABASE payment;
CREATE DATABASE auth;
